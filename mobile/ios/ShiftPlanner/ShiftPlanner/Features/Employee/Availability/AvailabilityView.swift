import SwiftUI

struct AvailabilityView: View {
    var body: some View {
        NavigationStack {
            Text("Employee availability")
                .navigationTitle("Availability")
        }
    }
}

