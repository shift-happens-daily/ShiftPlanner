import SwiftUI

struct ManagerScheduleView: View {
    var body: some View {
        NavigationStack {
            Text("Manager schedule info")
                .navigationTitle("Manager schedule")
        }
    }
}
