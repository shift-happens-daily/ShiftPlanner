import SwiftUI

struct RequirementsView: View {
    var body: some View {
        NavigationStack {
            Text("Shift requirements")
                .navigationTitle("Requirements")
        }
    }
}

