
import SwiftUI

struct EmployeeListView: View {
    var body: some View {
        NavigationStack {
            Text("Employee List")
                .navigationTitle("EmployeeList")
                .navigationBarTitleDisplayMode(.inline)
        }
    }
}
